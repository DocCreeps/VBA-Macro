Sub Valeur_cherchee()
Dim Destination As Worksheet, MaSource As Workbook, Source As Worksheet, i As Long, Trouve As Range
 
    Set Destination = ThisWorkbook.Sheets("Customers_Export(8)")
    Set MaSource = Workbooks.Open("C:\Users\dvericel\Desktop\Rapport.xls")
    Set Source = Workbooks(MaSource.Name).Sheets("Rapport")


Application.ScreenUpdating = False
    For i = 2 To 17

        Set Trouve = Source.Range("E" & j).Find(Destination.Range("C" & i), LookAt:=xlWhole)
        If Trouve Is Nothing Then
           Destination.Range("C" & i).Interior.Color = RGB(255, 0, 0)
        Else
             Destination.Range("C" & i).Interior.Color = RGB(0, 255, 0)
        End If
  
    Next
    
Application.ScreenUpdating = True
        Set Destination = Nothing: Set MaSource = Nothing: Set Source = Nothing
End Sub
